import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  screenContainer: {
    flex: 1,
    margin: 10,
  },
  formContainer: {
    flex: 1,
    alignItems: 'center',
  },
});
